# Task 004準備結果・次回仕様

2026-09-06。NG本体は無変更。今回の対象はテスト拡充と次回の変更範囲確定のみ。

## 追加検証

tests/ng.test.mjsへ3シナリオを追加し、原本と生成物の双方で実行した。

- 通常タグとロックタグの違い、大小文字正規化、NG登録/解除の伝播。タイトル変更通知がNG変更通知より先に発生すること。
- 広告ID/名前条件、未取得・照合成功・失敗、論理ルールの有効/無効と更新。広告通知時点では旧NG値、続いてNG変更通知が発生する現行順序。
- 数値比較6演算子の等値境界、タグの完全一致、タイトルの部分一致、欠損ID、空グループ、不正JSON。

結果: 全25件PASS、FAIL/SKIP 0。構文2ファイルPASS。生成物は原本とバイト一致。NG2件等の数え方はシナリオ単位であり、全条件の組合せ網羅を意味しない。

最初の試験実行ではテスト入力に通常オブジェクトのタグを渡し、Movieが必要とする通知メソッドがなく2件失敗した。テストを既存Tagモデル使用へ修正し、期待値・本体は変更せず全件再成功した。

## 維持すべき注意点

詳細未取得のNOT成立、欠損userIdの数値比較で0と等しいと評価される動き、空groupのNOTもfalseとなる動きを現行仕様として記録。改善案とは区別し、dev0で修正しない。

## 次回: Task 004A — 論理ルール定義の機械的抽出

設計キット06のTask 004（Evaluator抽出）は04のdev1と重複するため分割する。004Aはdev0のファイル分割、004Bはdev1の責務再設計。004Bは今回の準備完了を根拠に自動開始しない。

004Aの目的: 現行AdvancedNgRules定義だけをsrc/ng/logic-rules.jsへ移す。評価方法、呼出元Movie、イベント順、入力契約はそのまま。

許可変更:

- src/ng/logic-rules.js: AdvancedNgRulesの既存定義をバイトコピー。
- src/legacyの必要な前後断片: 元のコード順を保持するための境界追加のみ。
- scripts/build.mjs: 元の位置への連結指定。
- 対応するテスト、README、結果報告。

禁止: Movie/Tag/Contributorから判定責務を移す、evaluate APIの新設、正規化変更、通知や通信/DOM/cache/補充処理の変更、baseline編集、版表記変更。

手順: 作業前25件を確認→一意な定義境界で分割→元の位置へ連結→buildのバイト完全一致と構文確認→原本/生成物25件以上の試験→差分レビューとGit保存。

完了条件: 原本と生成物が463,894 bytes、SHA-256 AF382AE50FCF8CDFDFC2AE178F3BE8861AC7ECF1611F30F4E3FB94439A87F371で一致。テスト全件PASS。許可外の本体編集なし。実サイト未検証を明記。

## 将来の004Bに残る準備

実装を読み、旧形式ルールの移行、不正/境界入力、全演算子の真偽双方、タグ/投稿者更新時のイベント、詳細cache復元、広告取得を必要とする条件判定を追加確認する。既存の読み込み前後・失敗状態を表せる入力契約と、新旧比較経路を設計する。現行の25件だけで純粋Evaluatorへの変更を十分検証できるとは扱わない。

## 未実施

広告API実通信、実ブラウザ、実GM保存、cache/AutoFill/SPA統合は未実施。今回本体の分割も行っていない。ユーザーによる操作やスクリプト入れ替えは不要。
