# 実環境確認票（未実施）

Task 000ではインストールや実アカウント設定変更を行いません。後続の確認時に以下を使用します。

1. ブラウザとuserscript managerの名前・版、実行対象のハッシュを記録する。
2. 検証専用の環境を用意する。既存設定を保存する場合、CSVは7種のみで、ロックタグ・論理ルール・全設定のバックアップではない。manager側保存手段も確認する。
3. ranking、search、tagを一つずつ開き、URL、初期表示、ID順、コンソールエラーを記録する。
4. 再現用NG設定を入力し、対象動画のNG結果を確認する。詳細未取得と完了後を分ける。
5. AutoFillを試験条件で有効化し、目標、上限、追加ID、順序、停止理由を記録する。
6. 補充中と完了後にTile/Listを10往復し、追加要求・表示・詳細展開と▲▼を記録する。
7. 次ページ、最終ページ、戻る/進む、検索条件変更を試す。
8. 原本と変更版を同時に有効にせず、同一fixture/設定で比較する。

記録欄: ケースID / 日時 / 環境 / URL・fixture / 設定 / 原本の実測 / 変更版の実測 / 差分 / PASS・FAIL・未実施 / 証拠ファイル。

現在の全項目: 未実施。ユーザーが今行う操作はありません。
