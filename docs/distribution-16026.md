# 160.26 — 配布リポジトリの分離と固定ファイル名

2026-09-21。製品masterの `9a5bc4f` を基点とする。旧リポジトリの研究ブランチ・旧タグ・PRは、新しいリポジトリへ移さない。

## 配布と設定

- 主な配布ファイルは `dist/nico-nico-ranking-ng.user.js`。更新のたびにファイル名やREADMEのインストールリンクを変える必要はない。
- 従来の `dist/nico-nico-ranking-ng-v16-list-tile-fix.user.js` は互換用。同じビルドで同一内容を生成し、独立して編集しない。
- 版番号はメタデータの `@version` と内部診断の `NRN_VERSION` を160.26へ増番。スクリプトの名前・namespace・保存形式は維持。
- 新しい案内先は `ButaMonky/nico-nico-ranking-ng-fix`。自動更新は引き続き無効。旧版を削除せず、[移行手順](distribution.md#設定を保持して更新する)に従って手動更新する。

## 機能と制限

製品実行処理は160.25と同じ。[機能別の対応状況と利用上の制限](distribution-16025.md#機能の対応状況)を引き継ぐ。ホバープレビューは試験機能、連続再生は一部対応、個別ランキング・モバイル等の全条件は未確認。

## 検証

- 新URL、固定名、旧名とのバイト一致、160.26、既存識別値の保持の試験を追加・更新。実装前の失敗と実装後の成功を確認。
- 単体185件、構文検査、匿名診断の模擬ブラウザ試験1本（Edgeの一時プロファイル）が成功。
- 現在ツリーのプライバシー検査・差分の空白検査が成功。原本とHLS本体の固定ハッシュは維持。
- 新旧配布ファイルのSHA-256はともに `72dd43811391919c442ff59eecfd3c0262e600c2431689553cb5c9950d514a59`。
- 基点masterの61コミットを確認。履歴検査のメール条件に一致しない3件は、署名が有効なGitHubのWeb編集の公式記録者に限定。他の既存パターン検出はない。
- メール形式の追加検査では、URL偽装を拒否する試験データが8 blob版で一致した。実メールではないことを確認。拡張固有URLと既知の端末名は未検出。
- 別担当による静的レビューで修正指摘なし。新旧配布物の一致、任意出力先へのビルド、通知本文、実行処理を変更していないことを確認。

これらは今回実施した範囲であり、160.24の模擬ブラウザ21本や実サイトの全条件を今回再検証したという意味ではない。

## 公開前に残る確認

- [LICENSING](../LICENSING.md#確認が残るもの)に記録した、公式由来のSVG等、過去のスタイル、継承UTF変換の独自許諾。別リポジトリへの移行によって利用条件が確認済みになるわけではない。
- 実際のTampermonkeyでの旧版からの上書き、保存値保持、更新停止の確認。自動試験とは区別する。
- ニコニコ実サイトの全ページ・全アカウント条件での検証。

新リポジトリも初期状態は非公開とする。製品履歴のパターン検査だけで、公開・再配布に関するすべての問題がないと保証するものではない。
